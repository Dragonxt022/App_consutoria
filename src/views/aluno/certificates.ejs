<div class="space-y-6">
    <div class="bg-white shadow-xl rounded-2xl overflow-hidden">
        <div class="p-6 md:p-8 flex flex-col md:flex-row items-center justify-between bg-gradient-to-r from-emerald-500 to-teal-600 text-white">
            <div class="flex items-center space-x-4 md:space-x-6 mb-4 md:mb-0">
                <div class="relative">
                    <% if (user.avatar) { %>
                        <img class="h-16 w-16 md:h-20 md:w-20 rounded-full border-4 border-white/30 shadow-2xl object-cover" src="<%= user.avatar %>" alt="<%= user.name %>">
                    <% } else { %>
                        <div class="h-16 w-16 md:h-20 md:w-20 rounded-full border-4 border-white/30 shadow-2xl bg-white/10 flex items-center justify-center text-2xl md:text-3xl font-black">
                            <%= user.name.charAt(0).toUpperCase() %>
                        </div>
                    <% } %>
                    <div class="absolute -bottom-1 -right-1 h-5 w-5 bg-emerald-400 rounded-full border-2 border-white"></div>
                </div>
                <div>
                    <h2 class="text-2xl md:text-3xl font-extrabold">Meus Certificados</h2>
                    <p class="text-emerald-100 font-medium text-sm md:text-base">Visualize os certificados já liberados dos seus cursos concluídos.</p>
                </div>
            </div>
            <div class="flex flex-col items-start md:items-end space-y-2">
                <span class="text-xs font-bold uppercase tracking-widest text-emerald-200">Documentos do Aluno</span>
                <p class="text-xl font-black"><%= user.name %></p>
                <div class="flex space-x-2 md:space-x-3 mt-2 md:mt-4">
                    <a href="/meus-cursos" class="text-xs font-bold bg-white/10 hover:bg-white/20 px-3 py-2 rounded-lg transition-all">
                        Meus Cursos
                    </a>
                    <a href="/perfil" class="text-xs font-bold bg-white/10 hover:bg-white/20 px-3 py-2 rounded-lg transition-all">
                        Meu Perfil
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 md:gap-6">
        <div class="bg-white p-5 md:p-6 rounded-2xl shadow-sm border border-slate-100 hover:shadow-md transition-shadow">
            <div class="flex items-center space-x-4">
                <div class="bg-blue-50 p-3 rounded-xl text-blue-600">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
                </div>
                <div>
                    <p class="text-xs font-bold text-slate-400 uppercase tracking-tighter">Meus Cursos</p>
                    <h3 class="text-2xl font-black text-slate-800"><%= stats.totalCourses %></h3>
                </div>
            </div>
        </div>

        <div class="bg-white p-5 md:p-6 rounded-2xl shadow-sm border border-slate-100 hover:shadow-md transition-shadow">
            <div class="flex items-center space-x-4">
                <div class="bg-amber-50 p-3 rounded-xl text-amber-600">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
                </div>
                <div>
                    <p class="text-xs font-bold text-slate-400 uppercase tracking-tighter">Em Andamento</p>
                    <h3 class="text-2xl font-black text-slate-800"><%= stats.totalInProgress %></h3>
                </div>
            </div>
        </div>

        <div class="bg-white p-5 md:p-6 rounded-2xl shadow-sm border border-slate-100 hover:shadow-md transition-shadow">
            <div class="flex items-center space-x-4">
                <div class="bg-emerald-50 p-3 rounded-xl text-emerald-600">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                </div>
                <div>
                    <p class="text-xs font-bold text-slate-400 uppercase tracking-tighter">Certificados</p>
                    <h3 class="text-2xl font-black text-slate-800"><%= stats.totalCertificates %></h3>
                </div>
            </div>
        </div>
    </div>

    <div class="bg-white shadow-xl rounded-2xl overflow-hidden border border-slate-100">
        <div class="p-5 md:p-6 border-b border-slate-50 flex justify-between items-center bg-slate-50/50">
            <h3 class="font-black text-slate-800 uppercase tracking-tighter flex items-center text-lg md:text-xl">
                <svg class="w-5 h-5 mr-2 text-emerald-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                Certificados Disponíveis
            </h3>
            <span class="text-xs font-bold text-slate-500 bg-slate-100 px-3 py-1 rounded-full">
                <%= certificates.length %> certificado(s)
            </span>
        </div>

        <% if (!certificates.length) { %>
            <div class="p-8 md:p-12 text-center">
                <div class="bg-slate-50 w-16 h-16 md:w-20 md:h-20 rounded-full flex items-center justify-center mx-auto mb-4">
                    <svg class="w-8 h-8 md:w-10 md:h-10 text-slate-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                </div>
                <h4 class="text-slate-400 font-bold">Você ainda não possui certificados liberados.</h4>
                <p class="text-slate-400 text-sm mt-1">Quando um curso for concluído, o certificado aparecerá aqui automaticamente.</p>
            </div>
        <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-5 p-5 md:p-6">
                <% certificates.forEach((enroll) => { %>
                    <article class="rounded-2xl border border-slate-200 bg-slate-50 p-5 hover:shadow-md transition-shadow">
                        <div class="flex items-start justify-between gap-4">
                            <div>
                                <p class="text-xs font-black uppercase tracking-widest text-emerald-600">Curso concluído</p>
                                <h4 class="mt-3 text-lg font-bold text-slate-900 leading-snug"><%= enroll.Course ? enroll.Course.title : 'Curso indisponível' %></h4>
                            </div>
                            <div class="bg-emerald-100 text-emerald-700 rounded-xl p-3">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                </svg>
                            </div>
                        </div>

                        <div class="mt-4 text-sm text-slate-500 space-y-2">
                            <p><span class="font-semibold text-slate-700">Local:</span> <%= enroll.Course ? enroll.Course.location : '--' %></p>
                            <p><span class="font-semibold text-slate-700">Carga horária:</span> <%= enroll.Course ? enroll.Course.workload : '--' %></p>
                            <p><span class="font-semibold text-slate-700">Código:</span> <%= enroll.certificateCode || 'Será gerado ao abrir' %></p>
                        </div>

                        <div class="mt-5 flex gap-2">
                            <a href="/meu-certificado/<%= enroll.id %>" target="_blank" class="flex-1 bg-emerald-600 hover:bg-emerald-700 text-white px-4 py-3 rounded-lg text-xs font-bold shadow-lg shadow-emerald-100 transition-all text-center">
                                Abrir Certificado
                            </a>
                            <a href="/meu-comprovante/<%= enroll.id %>" target="_blank" class="bg-white hover:bg-slate-100 text-slate-700 px-4 py-3 rounded-lg text-xs font-bold border border-slate-200 transition-all">
                                Comprovante
                            </a>
                        </div>
                    </article>
                <% }) %>
            </div>
        <% } %>
    </div>
</div>
