<link href="https://cdn.quilljs.com/1.3.7/quill.snow.css" rel="stylesheet">

<div class="w-full max-w-6xl mr-auto">
    <div class="bg-white shadow-xl rounded-2xl overflow-hidden">
        <form action="/admin/cursos/<%= course.id %>/editar" method="POST" enctype="multipart/form-data" class="p-8 space-y-8">
            <div class="rounded-2xl border border-slate-200 bg-slate-50/80 px-5 py-4 flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between">
                <div>
                    <p class="text-sm font-semibold text-slate-800">Editando: <span class="text-slate-950"><%= course.title %></span></p>
                    <p class="text-sm text-slate-500">Revise o conteúdo, os dados do treinamento e os arquivos de apoio sem repetir informações no topo da página.</p>
                </div>
                <div class="flex flex-wrap items-center gap-2">
                    <span class="inline-flex items-center rounded-full bg-white px-3 py-1 text-[11px] font-bold uppercase tracking-wider text-slate-600 border border-slate-200">
                        Campos com * são obrigatórios
                    </span>
                    <% if (course.statusCode === 'desativado') { %>
                        <span class="bg-red-100 text-red-700 px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest border border-red-200">Desativado</span>
                    <% } else if (course.statusCode === 'encerrado') { %>
                        <span class="bg-amber-100 text-amber-700 px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest border border-amber-200">Encerrado</span>
                    <% } else if (course.statusCode === 'confirmado') { %>
                        <span class="border border-blue-700 bg-blue-600 px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-[0.18em] text-white shadow-sm shadow-blue-200">Confirmado</span>
                    <% } else { %>
                        <span class="bg-emerald-100 text-emerald-700 px-3 py-1 rounded-full text-[10px] font-black uppercase tracking-widest border border-emerald-200">Ativo</span>
                    <% } %>
                </div>
            </div>

            <div class="grid gap-8 xl:grid-cols-[1.45fr_0.95fr]">
                <section class="space-y-8">
                    <div class="rounded-2xl border border-slate-200 bg-slate-50/70 p-6">
                        <div class="mb-5">
                            <h3 class="text-lg font-bold text-slate-800">Conteúdo principal</h3>
                            <p class="text-sm text-slate-500">Área ampliada para revisar a apresentação comercial e técnica do curso.</p>
                        </div>

                        <div class="space-y-6">
                            <div>
                                <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Nome do Curso *</label>
                                <input type="text" name="title" value="<%= course.title %>" required class="w-full px-4 py-3 rounded-xl border border-slate-200 bg-white focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all" placeholder="Ex: Gestão Estratégica 4.0">
                            </div>

                            <div>
                                <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Descrição do Curso *</label>
                                <p class="text-xs text-slate-400 mb-3">Revise com calma o texto exibido na vitrine e na página do curso. Este campo agora tem mais espaço para edição.</p>
                                <input type="hidden" name="description" id="descriptionInput" required>
                                <div id="courseDescriptionEditor" class="rounded-2xl border border-slate-200 bg-white min-h-[280px]"><%- course.description || '' %></div>
                            </div>
                        </div>
                    </div>

                    <div class="rounded-2xl border border-slate-200 bg-white p-6">
                        <div class="mb-5 flex items-start justify-between gap-4">
                            <div>
                                <h3 class="text-lg font-bold text-slate-800">Verso do certificado</h3>
                                <p class="text-sm text-slate-500">Ajuste o conteúdo com mais controle visual. O resumo interno continua sendo gerado automaticamente a partir desse conteúdo.</p>
                            </div>
                            <span class="inline-flex items-center rounded-full bg-indigo-50 px-3 py-1 text-[11px] font-bold uppercase tracking-wider text-indigo-700">Editor avançado</span>
                        </div>

                        <div>
                            <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Conteúdo formatado do verso</label>
                            <p class="text-xs text-slate-400 mb-3">Use títulos, listas e destaques para definir melhor como o verso será exibido no certificado.</p>
                            <input type="hidden" name="certificateBackContent" id="certificateBackContentInput">
                            <div id="certificateBackContentEditor" class="rounded-2xl border border-slate-200 bg-white min-h-[280px]"><%- course.certificateBackContentHtml || '' %></div>
                        </div>
                    </div>
                </section>

                <section class="space-y-8">
                    <div class="rounded-2xl border border-slate-200 bg-white p-6">
                        <div class="mb-5">
                            <h3 class="text-lg font-bold text-slate-800">Dados do treinamento</h3>
                            <p class="text-sm text-slate-500">Agenda, local, valor, vagas e status de publicacao.</p>
                        </div>

                        <div class="space-y-6">
                            <div>
                                <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Local do Treinamento *</label>
                                <input type="text" name="location" value="<%= course.location %>" required class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all" placeholder="Ex: Hotel Hilton, São Paulo - SP">
                            </div>

                            <div class="grid sm:grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Data de Início *</label>
                                    <% 
                                        let dateVal = '';
                                        if(course.startDate) {
                                            const d = new Date(course.startDate);
                                            dateVal = d.toISOString().split('T')[0];
                                        }
                                    %>
                                    <input type="date" name="startDate" value="<%= dateVal %>" required class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all">
                                </div>
                                <div>
                                    <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Carga Horária *</label>
                                    <input type="text" name="workload" value="<%= course.workload %>" required class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all" placeholder="Ex: 40 horas">
                                </div>
                            </div>

                            <div class="grid sm:grid-cols-2 gap-4">
                                <div>
                                    <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Investimento (R$) *</label>
                                    <input type="text" name="price" value="<%= course.priceInputValue %>" required data-currency-input data-currency-prefix="R$ " data-currency-storage="decimal" data-currency-mode="cents" inputmode="numeric" autocomplete="off" class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all" placeholder="R$ 1.690,00">
                                    <p class="mt-2 text-xs text-slate-400">Digite normalmente que o valor se ajusta automaticamente.</p>
                                </div>
                                <div>
                                    <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Vagas Disponíveis *</label>
                                    <input type="number" name="spots" value="<%= course.spots %>" required class="w-full px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-all" placeholder="20">
                                </div>
                            </div>

                            <div>
                                <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Status Manual do Curso</label>
                                <select name="status" class="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-500 transition-all">
                                    <option value="ativo" <%= course.status === 'ativo' ? 'selected' : '' %>>Ativo</option>
                                    <option value="confirmado" <%= course.status === 'confirmado' ? 'selected' : '' %>>Confirmado</option>
                                </select>
                                <p class="mt-2 text-xs text-slate-400">Esse status é manual e aparece no site público e no portal do aluno quando o curso estiver ativo e ainda não tiver encerrado.</p>
                            </div>

                            <div class="pt-2">
                                <label class="flex items-center space-x-3 text-sm font-bold text-slate-700 cursor-pointer p-4 bg-slate-50 rounded-xl border border-slate-100 hover:bg-slate-100 transition-all">
                                    <input type="checkbox" name="active" <%= course.active ? 'checked' : '' %> class="w-5 h-5 text-emerald-600 rounded border-slate-300 focus:ring-emerald-500">
                                    <span>Curso visível e aberto para inscrições</span>
                                </label>
                                <p class="text-[10px] text-slate-400 mt-2 px-1">Ao desmarcar, o curso deixa de aparecer na página inicial e nos detalhes.</p>
                            </div>
                        </div>
                    </div>

                    <div class="rounded-2xl border border-slate-200 bg-white p-6">
                        <div class="mb-5">
                            <h3 class="text-lg font-bold text-slate-800">Itens inclusos</h3>
                            <p class="text-sm text-slate-500">Ajuste a lista de benefícios entregues ao participante.</p>
                        </div>

                        <label class="block text-xs font-bold text-slate-500 uppercase mb-2">O que está incluso?</label>
                        <p class="text-xs text-slate-400 mb-3">Digite um item e aperte <span class="font-semibold">Enter</span> para adicionar.</p>

                        <div class="flex flex-col sm:flex-row items-stretch sm:items-center gap-3">
                            <input id="includeInput" type="text" placeholder="Ex: Material didatico completo" class="flex-1 px-4 py-3 rounded-xl border border-slate-200 focus:ring-2 focus:ring-indigo-500 outline-none">
                            <button type="button" id="addIncludeBtn" class="px-5 py-3 bg-indigo-600 text-white rounded-xl hover:bg-indigo-700">Adicionar</button>
                        </div>

                        <ul id="includesList" class="mt-4 space-y-2 max-h-80 overflow-y-auto pr-1">
                            <% 
                                const initialIncludes = (course.itemsIncluded && Array.isArray(course.itemsIncluded) && course.itemsIncluded.length) ? course.itemsIncluded : [];
                                if (initialIncludes.length === 0) {
                            %>
                                <li class="flex items-center justify-between bg-slate-50 p-3 rounded-lg">
                                    <div class="flex items-center gap-3">
                                        <input type="checkbox" name="itemsIncluded[]" value="COPO E CANETA." checked class="w-4 h-4 text-indigo-600">
                                        <span class="text-sm text-slate-700">COPO E CANETA.</span>
                                    </div>
                                    <button type="button" class="removeInclude text-red-500 hover:text-red-600">Remover</button>
                                </li>
                                <li class="flex items-center justify-between bg-slate-50 p-3 rounded-lg">
                                    <div class="flex items-center gap-3">
                                        <input type="checkbox" name="itemsIncluded[]" value="COFFEE-BREAK." checked class="w-4 h-4 text-indigo-600">
                                        <span class="text-sm text-slate-700">COFFEE-BREAK.</span>
                                    </div>
                                    <button type="button" class="removeInclude text-red-500 hover:text-red-600">Remover</button>
                                </li>
                                <li class="flex items-center justify-between bg-slate-50 p-3 rounded-lg">
                                    <div class="flex items-center gap-3">
                                        <input type="checkbox" name="itemsIncluded[]" value="CERTIFICADO DE PARTICIPAÇÃO." checked class="w-4 h-4 text-indigo-600">
                                        <span class="text-sm text-slate-700">CERTIFICADO DE PARTICIPAÇÃO.</span>
                                    </div>
                                    <button type="button" class="removeInclude text-red-500 hover:text-red-600">Remover</button>
                                </li>
                            <% } else { %>
                                <% initialIncludes.forEach(function(item) { %>
                                    <li class="flex items-center justify-between bg-slate-50 p-3 rounded-lg">
                                        <div class="flex items-center gap-3">
                                            <input type="checkbox" name="itemsIncluded[]" value="<%= item %>" checked class="w-4 h-4 text-indigo-600">
                                            <span class="text-sm text-slate-700"><%= item %></span>
                                        </div>
                                        <button type="button" class="removeInclude text-red-500 hover:text-red-600">Remover</button>
                                    </li>
                                <% }); %>
                            <% } %>
                        </ul>
                    </div>
                </section>
            </div>

            <div class="pt-8 border-t border-slate-100 flex flex-col md:flex-row gap-8">
                <div class="flex-1">
                    <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Imagem de Capa</label>
                    <div class="relative border-2 border-dashed border-slate-200 rounded-2xl p-6 text-center hover:border-indigo-400 transition-all">
                        <input type="file" name="image" id="imageInput" accept="image/*" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer">
                        <div id="imagePreview" class="<%= course.image ? '' : 'hidden' %> mb-4">
                            <div class="relative inline-block">
                                <img id="previewImage" class="max-h-48 mx-auto rounded-lg shadow-md" src="<%= course.image ? course.image : '#' %>" alt="Pré-visualização da imagem">
                                <button type="button" id="removeImage" class="absolute -top-2 -right-2 bg-red-500 text-white rounded-full w-6 h-6 flex items-center justify-center hover:bg-red-600 transition-colors">
                                    <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                                    </svg>
                                </button>
                            </div>
                        </div>
                        <div id="imagePlaceholder" class="<%= course.image ? 'hidden' : '' %>">
                            <svg class="w-10 h-10 text-slate-400 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                            </svg>
                            <p class="text-xs text-slate-500">Arraste ou clique para selecionar (JPG, PNG)</p>
                            <p class="text-xs text-slate-400 mt-1">Recomendado: 1200x630 pixels</p>
                        </div>
                    </div>
                </div>

                <div class="flex-1">
                    <label class="block text-xs font-bold text-slate-500 uppercase mb-2">Proposta do Curso (PDF/DOC/DOCX)</label>
                    <div class="relative border-2 border-dashed border-slate-200 rounded-2xl p-6 text-center hover:border-indigo-400 transition-all">
                        <input type="file" name="proposalDoc" id="pdfInput" accept=".pdf,.doc,.docx" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer">
                        <div id="pdfPreview" class="<%= course.proposalDoc ? '' : 'hidden' %> mb-4">
                            <div class="bg-slate-50 rounded-lg p-4">
                                <div class="flex items-center justify-between">
                                    <div class="flex items-center space-x-3 overflow-hidden">
                                        <svg class="w-8 h-8 text-red-500 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                                        </svg>
                                        <div class="overflow-hidden">
                                            <p id="pdfFileName" class="text-sm font-medium text-slate-700 truncate max-w-xs">
                                                <%= course.proposalDoc ? course.proposalDoc.split('/').pop() : '' %>
                                            </p>
                                            <p id="pdfFileSize" class="text-xs text-slate-500">Arquivo atual</p>
                                        </div>
                                    </div>
                                    <button type="button" id="removePdf" class="text-red-500 hover:text-red-600 flex-shrink-0">
                                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                                        </svg>
                                    </button>
                                </div>
                            </div>
                        </div>
                        <div id="pdfPlaceholder" class="<%= course.proposalDoc ? 'hidden' : '' %>">
                            <svg class="w-10 h-10 text-slate-400 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                            </svg>
                            <p class="text-xs text-slate-500">Selecione o documento da proposta</p>
                            <p class="text-xs text-slate-400 mt-1">Formatos aceitos: PDF, DOC e DOCX até 8MB</p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="flex justify-end space-x-4 pt-4">
                <a href="/admin/cursos" class="px-6 py-3 text-slate-600 font-bold text-sm">Cancelar</a>
                <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white px-10 py-3 rounded-xl text-sm font-bold shadow-lg shadow-indigo-100 transform active:scale-95 transition-all">
                    Salvar Alterações
                </button>
            </div>
        </form>
    </div>
</div>

<script src="https://cdn.quilljs.com/1.3.7/quill.min.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function() {
    const descriptionEditor = new Quill('#courseDescriptionEditor', {
        theme: 'snow',
        placeholder: 'Resumo do que sera abordado no curso...',
        modules: {
            toolbar: [
                [{ header: [1, 2, 3, false] }],
                ['bold', 'italic', 'underline'],
                [{ color: [] }, { background: [] }],
                [{ list: 'ordered' }, { list: 'bullet' }],
                ['blockquote', 'link'],
                ['clean']
            ]
        }
    });
    const descriptionInput = document.getElementById('descriptionInput');
    descriptionInput.value = descriptionEditor.root.innerHTML;
    descriptionEditor.on('text-change', function() {
        descriptionInput.value = descriptionEditor.root.innerHTML;
    });

    const certificateBackEditor = new Quill('#certificateBackContentEditor', {
        theme: 'snow',
        placeholder: 'Estruture o conteúdo do verso do certificado...',
        modules: {
            toolbar: [
                [{ header: [2, 3, false] }],
                ['bold', 'italic', 'underline'],
                [{ list: 'ordered' }, { list: 'bullet' }],
                [{ color: [] }],
                ['blockquote', 'clean']
            ]
        }
    });
    const certificateBackInput = document.getElementById('certificateBackContentInput');
    certificateBackInput.value = certificateBackEditor.root.innerHTML;
    certificateBackEditor.on('text-change', function() {
        certificateBackInput.value = certificateBackEditor.root.innerHTML;
    });

    const imageInput = document.getElementById('imageInput');
    const imagePreview = document.getElementById('imagePreview');
    const previewImage = document.getElementById('previewImage');
    const imagePlaceholder = document.getElementById('imagePlaceholder');
    const removeImageBtn = document.getElementById('removeImage');
    const pdfInput = document.getElementById('pdfInput');
    const pdfPreview = document.getElementById('pdfPreview');
    const pdfFileName = document.getElementById('pdfFileName');
    const pdfFileSize = document.getElementById('pdfFileSize');
    const pdfPlaceholder = document.getElementById('pdfPlaceholder');
    const removePdfBtn = document.getElementById('removePdf');

    imageInput.addEventListener('change', function(e) {
        const file = e.target.files[0];
        if (file) {
            if (!file.type.match('image.*')) {
                window.appModal.alert('Por favor, selecione apenas arquivos de imagem.', { title: 'Arquivo inválido', variant: 'warning' });
                imageInput.value = '';
                return;
            }
            if (file.size > 5 * 1024 * 1024) {
                window.appModal.alert('A imagem não pode ter mais de 5MB.', { title: 'Imagem muito grande', variant: 'warning' });
                imageInput.value = '';
                return;
            }
            const reader = new FileReader();
            reader.onload = function(event) {
                previewImage.src = event.target.result;
                imagePreview.classList.remove('hidden');
                imagePlaceholder.classList.add('hidden');
            };
            reader.readAsDataURL(file);
        }
    });

    removeImageBtn.addEventListener('click', function() {
        imageInput.value = '';
        previewImage.src = '#';
        imagePreview.classList.add('hidden');
        imagePlaceholder.classList.remove('hidden');
    });

    pdfInput.addEventListener('change', function(e) {
        const file = e.target.files[0];
        if (file) {
            const validExtensions = ['.pdf', '.doc', '.docx'];
            const fileName = file.name.toLowerCase();
            const isValid = validExtensions.some((ext) => fileName.endsWith(ext));
            if (!isValid) {
                window.appModal.alert('Por favor, selecione apenas arquivos PDF, DOC ou DOCX.', { title: 'Arquivo inválido', variant: 'warning' });
                pdfInput.value = '';
                return;
            }
            if (file.size > 8 * 1024 * 1024) {
                window.appModal.alert('O documento não pode ter mais de 8MB.', { title: 'Documento muito grande', variant: 'warning' });
                pdfInput.value = '';
                return;
            }
            pdfFileName.textContent = file.name;
            const fileSize = file.size;
            let sizeText;
            if (fileSize < 1024) sizeText = fileSize + ' bytes';
            else if (fileSize < 1024 * 1024) sizeText = (fileSize / 1024).toFixed(1) + ' KB';
            else sizeText = (fileSize / (1024 * 1024)).toFixed(1) + ' MB';
            pdfFileSize.textContent = sizeText;
            pdfPreview.classList.remove('hidden');
            pdfPlaceholder.classList.add('hidden');
        }
    });

    removePdfBtn.addEventListener('click', function() {
        pdfInput.value = '';
        pdfFileName.textContent = '';
        pdfFileSize.textContent = '';
        pdfPreview.classList.add('hidden');
        pdfPlaceholder.classList.remove('hidden');
    });

    const imageDropArea = imageInput.parentElement;
    ['dragenter', 'dragover', 'dragleave', 'drop'].forEach((eventName) => {
        imageDropArea.addEventListener(eventName, preventDefaults, false);
    });

    function preventDefaults(e) {
        e.preventDefault();
        e.stopPropagation();
    }

    ['dragenter', 'dragover'].forEach((eventName) => {
        imageDropArea.addEventListener(eventName, () => imageDropArea.classList.add('border-indigo-500', 'bg-indigo-50'), false);
    });

    ['dragleave', 'drop'].forEach((eventName) => {
        imageDropArea.addEventListener(eventName, () => imageDropArea.classList.remove('border-indigo-500', 'bg-indigo-50'), false);
    });

    imageDropArea.addEventListener('drop', function(e) {
        const dt = e.dataTransfer;
        imageInput.files = dt.files;
        imageInput.dispatchEvent(new Event('change'));
    }, false);

    const pdfDropArea = pdfInput.parentElement;
    ['dragenter', 'dragover', 'dragleave', 'drop'].forEach((eventName) => {
        pdfDropArea.addEventListener(eventName, preventDefaults, false);
    });

    ['dragenter', 'dragover'].forEach((eventName) => {
        pdfDropArea.addEventListener(eventName, () => pdfDropArea.classList.add('border-indigo-500', 'bg-indigo-50'), false);
    });

    ['dragleave', 'drop'].forEach((eventName) => {
        pdfDropArea.addEventListener(eventName, () => pdfDropArea.classList.remove('border-indigo-500', 'bg-indigo-50'), false);
    });

    pdfDropArea.addEventListener('drop', function(e) {
        const dt = e.dataTransfer;
        pdfInput.files = dt.files;
        pdfInput.dispatchEvent(new Event('change'));
    }, false);

    const includeInput = document.getElementById('includeInput');
    const addIncludeBtn = document.getElementById('addIncludeBtn');
    const includesList = document.getElementById('includesList');

    function createIncludeItem(text) {
        const li = document.createElement('li');
        li.className = 'flex items-center justify-between bg-slate-50 p-3 rounded-lg';
        const left = document.createElement('div');
        left.className = 'flex items-center gap-3';
        const checkbox = document.createElement('input');
        checkbox.type = 'checkbox';
        checkbox.name = 'itemsIncluded[]';
        checkbox.value = text;
        checkbox.checked = true;
        checkbox.className = 'w-4 h-4 text-indigo-600';
        const span = document.createElement('span');
        span.className = 'text-sm text-slate-700';
        span.textContent = text;
        left.appendChild(checkbox);
        left.appendChild(span);
        const removeBtn = document.createElement('button');
        removeBtn.type = 'button';
        removeBtn.className = 'removeInclude text-red-500 hover:text-red-600';
        removeBtn.textContent = 'Remover';
        removeBtn.addEventListener('click', function() { li.remove(); });
        li.appendChild(left);
        li.appendChild(removeBtn);
        return li;
    }

    function addIncludeFromInput() {
        const text = (includeInput.value || '').trim();
        if (!text) return;
        includesList.appendChild(createIncludeItem(text));
        includeInput.value = '';
        includeInput.focus();
    }

    addIncludeBtn.addEventListener('click', addIncludeFromInput);
    includeInput.addEventListener('keydown', function(e) {
        if (e.key === 'Enter') {
            e.preventDefault();
            addIncludeFromInput();
        }
    });

    document.querySelectorAll('.removeInclude').forEach((btn) => {
        btn.addEventListener('click', function() {
            const li = btn.closest('li');
            if (li) li.remove();
        });
    });
});
</script>
